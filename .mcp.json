{
  "name": "pdf-document-mcp",
  "description": "> **By [MEOK AI Labs](https://meok.ai)** \u2014 Document processing toolkit: extract text from PDFs, convert to Markdown, merge files, extract table",
  "version": "1.0.0",
  "tools": [
    {
      "name": "extract_text_from_pdf",
      "description": "Extract text content from a PDF file. Optionally specify page numbers",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path": {
            "type": "string"
          },
          "pages": {
            "type": "integer"
          }
        },
        "required": [
          "file_path"
        ]
      }
    },
    {
      "name": "convert_pdf_to_markdown",
      "description": "Convert a PDF document to Markdown format. Detects headings based on",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path": {
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ]
      }
    },
    {
      "name": "merge_pdfs",
      "description": "Merge multiple PDF files into a single document.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_paths": {
            "type": "array"
          },
          "output_path": {
            "type": "string"
          }
        },
        "required": [
          "file_paths",
          "output_path"
        ]
      }
    },
    {
      "name": "extract_tables",
      "description": "Extract table-like structures from a specific page in a PDF.",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path": {
            "type": "string"
          },
          "page_num": {
            "type": "integer"
          }
        },
        "required": [
          "file_path"
        ]
      }
    },
    {
      "name": "summarize_document",
      "description": "Generate a structural summary of a PDF: metadata, statistics (pages,",
      "parameters": {
        "type": "object",
        "properties": {
          "file_path": {
            "type": "string"
          }
        },
        "required": [
          "file_path"
        ]
      }
    }
  ]
}